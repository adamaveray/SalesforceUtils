{
  "scripts": {
    "test": "vendor/bin/phpunit --configuration tests/phpunit.xml && npm run format-check",
    "format": "prettier --write 'src/**/*.{php,xml}' 'tests/**/*.{php,xml}' package.json composer.json './.*.{json,yml}'",
    "format-check": "prettier --check 'src/**/*.{php,xml}' 'tests/**/*.{php,xml}' package.json composer.json './.*.{json,yml}'"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.16.0",
    "@prettier/plugin-xml": "^0.12.0",
    "prettier": "^2.1.2"
  }
}
